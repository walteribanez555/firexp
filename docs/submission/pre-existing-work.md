# Pre-existing Work Disclosure (draft)

_Devpost required field: disclose any work that predates the hackathon window and what was built/changed during it._

**Team:** solo developer (walteribanez555).

**Statement.** Firexp is **original work built during the hackathon window** (the project's public Git history begins with the initial commit in this repository — see `git log`). No pre-existing product, codebase, or design was carried in; the concept, the branching engine, all apps, the CMS, and the infrastructure-as-code were created within the submission period.

**Third-party components** used under their licenses (nothing built by a third party is claimed as our own):
- Open-source libraries: Kotlin/Jetpack Compose, Media3/ExoPlayer, Hono, React, shadcn/ui, React Flow, AWS SDKs, AWS CDK, zod, Vite, Turborepo.
- **Test media** (not committed to the repo): sample clips from test-videos.co.uk (Big Buck Bunny, Sintel, Jellyfish — CC) and placeholder posters from Lorem Picsum, used only for local testing. The final demo video will use content produced or licensed for the submission (no third-party footage/music without permission).

**How to verify scope:** the entire history is in this repo (`git log --reverse`); the first commit corresponds to the initial upload during the window.

> TODO before submitting: if any commit predates the official window start, replace this with a precise list of what changed inside the window + a link to the commit range.
