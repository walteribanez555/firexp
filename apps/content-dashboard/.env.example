# Base URL of the content-api backend (must end without a trailing slash).
# Used by the dashboard to fetch series, episodes, and generate presigned upload URLs.
VITE_CONTENT_API_URL=http://localhost:3003/api/v1
